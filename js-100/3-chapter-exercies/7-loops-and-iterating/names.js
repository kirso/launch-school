let names = ["Hello", "this", "is", "an", "array"]; // create array
let upperNames = [];
let index = 0;
//condition

// // execute the loop until we reach the last item in the index.
// while (index < names.length) {
// 	// access the name stored at name[index] and call a method to return capitalised.Original array stays the same.
// 	let upperCaseName = names[index].toUpperCase();
// 	upperNames.push(upperCaseName);
// 	index += 1;
// }

// console.log(upperNames);

// let answer = 0;

// do {
// 	answer += 1;
// } while (answer < 10);

// console.log(answer);

// for (let i = 0; i < names.length; i += 1) {
// 	let upperCaseName = names[i].toUpperCase();
// 	upperNames.push(upperCaseName);
// }

// console.log(upperNames);
