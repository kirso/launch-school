// We can access array elements with bracket notation via specifying an index. Arrays are zero-based, meaning first element starts at 0 and continues with 1,2,3 etc.

let arr = ["fish", "and", "chips"];

console.log(arr[1]);
