// Explain why this code logs '510' instead of 15.

// '5' is a string, when using + sign it concatinates the two together as string overrules instead of adding numbers together.
console.log("5" + 10)
